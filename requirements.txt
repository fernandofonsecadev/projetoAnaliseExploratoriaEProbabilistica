# Dependencias do projeto de Analise Exploratoria e Probabilistica (Churn)
# Instale com:  python -m pip install -r requirements.txt
#
# Versoes minimas (>=) em vez de fixas, para instalar em qualquer maquina/Python 3.9+.
# os e warnings NAO entram aqui (fazem parte da biblioteca padrao do Python).

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
joblib>=1.3
streamlit>=1.30
ipykernel>=6.0
